<div class="o-wrapper">
  <section class="o-simple-block">
    <h1 class="o-simple-block__title">{{ "Page introuvable" | t }}</h1>
    <div class="c-message c-message--notice">
      <div class="c-message__description">
        <i class="icon icon-warning"></i> {{ "La page que vous avez demandée n'existe pas." | t }}
      </div>
    </div>

    <div class="c-button--center">
      <a class="o-button c-button-color-red" href="{{ localized_root_path }}">
        {{ "Retour à l'accueil" | t }}
      </a>
    </div>
  </section>
</div>
